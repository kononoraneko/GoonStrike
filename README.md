# GoonStrike

See `AGENTS.md` for project architecture notes for AI agents.

Dedicated server and backend setup: `docs/dedicated_server.md`.

Registry admin panel (separate frontend): `admin-panel/index.html` (run with `python -m http.server 5173`).
Docker option: `docker compose up -d admin-panel` then open `http://127.0.0.1:5173`.
